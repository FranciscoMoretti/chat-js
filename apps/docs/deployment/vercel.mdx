---
title: Deploy to Vercel
description: Production deployment with Vercel's integrated platform
---

You can deploy the Next.js application and its integrated EVE worker to one Vercel project. Configure your application database, private EVE credentials, and OAuth origins before deploying. Registered and guest agents use managed Vercel Workflow automatically.

<Callout type="note">
  If you haven't set up your project yet, start with the
  [Quickstart](/quickstart) to scaffold and run locally first.
</Callout>

## Vercel Integrations

ChatJS uses several Vercel platform features. Enable them in your Vercel dashboard under **Storage** and **AI**.

| Integration | Purpose | Required |
| --- | --- | --- |
| **PostgreSQL** (for example, Neon) | Application database | Yes |
| **Vercel Workflow** | EVE execution and durable transcripts | Selected automatically |
| **Vercel Blob** | File attachments, generated images | If using attachments/image gen |
| **AI Gateway** | Unified access to 120+ AI models | Yes |

### AI Gateway

By default, ChatJS uses [Vercel AI Gateway](/gateways/vercel) to access 120+ models from OpenAI, Anthropic, Google, and more.

1. Go to [Vercel AI Gateway](https://vercel.com/ai-gateway)
2. Create an API key
3. Add to environment: `AI_GATEWAY_API_KEY`

You can switch to [OpenRouter](/gateways/openrouter) or any other [supported gateway](/gateways/overview) in `chat.config.ts`. See [Multi-Model Support](/core/multi-model) for configuring available models.

### Blob Storage

Vercel Blob is the recommended Files SDK provider for file attachments and generated media on Vercel. See [File storage](../storage) for the provider-agnostic architecture and migration behavior.

1. Go to **Storage** → **Create** → **Blob**
2. Connect to your project
3. Environment variable `BLOB_READ_WRITE_TOKEN` is auto-added

To disable blob features, set in `chat.config.ts`:

```typescript
features: {
  attachments: false,
},
ai: {
  tools: {
    image: { enabled: false },
    video: { enabled: false },
  },
}
```

## Cron Jobs

ChatJS includes a daily cleanup job that removes orphaned managed files uploaded but never saved to a message.

### Configuration

Defined in `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "0 2 * * *"
    }
  ]
}
```

Runs daily at 2 AM UTC. Adjust the schedule using [cron syntax](https://crontab.guru/).

### Security

The cron endpoint requires a `CRON_SECRET` environment variable:

```bash
# Generate a secret
openssl rand -base64 32
```

Add to Vercel environment variables. Vercel automatically sends this as a Bearer token.

### Customizing Cleanup

Edit `app/api/cron/cleanup/route.ts` to add cleanup tasks:

```typescript
const results = {
  orphanedAttachments: await cleanupOrphanedAttachments(),
  // Add other cleanup tasks here
  expiredSessions: await cleanupExpiredSessions(),
};
```

## Code Execution Sandbox

The code execution tool uses [Vercel Sandbox](https://vercel.com/docs/functions/og-image-generation) for secure Python execution.

### Authentication

On Vercel, sandbox uses OIDC automatically. For local development or self-hosted:

```bash
VERCEL_TEAM_ID=team_xxx
VERCEL_PROJECT_ID=prj_xxx
VERCEL_TOKEN=xxx
```

### Runtime Configuration

Set the Python version via environment variable:

```bash
VERCEL_SANDBOX_RUNTIME=python3.13  # default
```

### Resource Limits

Sandboxes run with:

- 2 vCPUs
- 5 minute timeout
- Pre-installed: matplotlib, pandas, numpy, sympy, yfinance

## Environment Variables

### Required

Configure the credentials listed in [Environment Variables](../reference/env-vars), including `DATABASE_URL`, `EVE_GATEWAY_SECRET`, and `EVE_INTERNAL_ORIGIN`. EVE settings must be available during the build and at runtime.

Initialize the application schema using the [database setup guide](../reference/database#hosted-workflow-database). Use separate application databases for previews and production, with branch-scoped preview credentials. Omit `WORKFLOW_POSTGRES_URL` on Vercel. It is required only for local/self-hosted registered-chat workflows.

Backend selection uses the Vercel platform environment and is shared by EVE compilation, ChatJS runtime checks, and setup. Local development remains on PostgreSQL. Rebuild for the destination environment rather than moving a self-hosted build onto Vercel.

Production Vercel builds apply application migrations automatically. Preview builds skip them, so run those migrations explicitly before testing a preview. Managed Workflow does not require PostgreSQL workflow provisioning.

### Existing PostgreSQL workflow deployments

Switching backends does not move existing sessions or their transcripts. Validate the new deployment against an isolated application database first. Existing PostgreSQL sessions need a separate drain or migration plan before production cutover. Do not reuse their application bindings as though the managed backend contained those runs.

Billing retains application-owned cursors and reads unread event suffixes through EVE on Vercel. The local PostgreSQL stream-position optimization is not used. Managed reconciliation reads each bound session with at most four concurrent requests.

Hosted conversation erasure remains unavailable in the installed EVE version. ChatJS rejects deletion before revoking access, except for unaccepted copies that never created a native session. A managed retention policy is not a substitute for verified deletion.

### Preview origins and OAuth

Use a stable branch hostname for preview sign-in. Without an explicit `APP_URL`, ChatJS uses `VERCEL_BRANCH_URL` when `VERCEL_ENV=preview`, then falls back to `VERCEL_URL`. `APP_URL` takes precedence in every environment. Remove an inherited production or localhost value from preview settings.

Register `https://<branch-hostname>/api/auth/callback/<provider>` in each enabled OAuth provider. For production, set `APP_URL` to your public domain and register callbacks against that domain. If `APP_URL` is unset on a production Vercel deployment, Better Auth uses `appUrl` from `chat.config.ts` instead. A successful preview deployment does not configure these provider allowlists.

Set `EVE_INTERNAL_ORIGIN` separately for each environment. With the integrated worker, it can use the stable branch origin for previews. It is not automatically derived from `APP_URL`.

### Protected previews

Enable **Protection Bypass for Automation** in the Vercel project and make its `VERCEL_AUTOMATION_BYPASS_SECRET` available to the deployment. Redeploy after changing environment settings. Keep this credential server-only.

ChatJS forwards it as `x-vercel-protection-bypass` only when `EVE_INTERNAL_ORIGIN` exactly matches the deployment hostname (`VERCEL_URL`) or branch hostname (`VERCEL_BRANCH_URL`). A separate EVE worker and arbitrary custom domains do not receive this project credential. This bypass passes Vercel protection only: ChatJS still sends the EVE bearer secret and owner identity.

### Optional (Vercel Features)

| Variable                | Feature                                |
| ----------------------- | -------------------------------------- |
| `BLOB_READ_WRITE_TOKEN` | Blob storage (auto-set by integration) |
| `CRON_SECRET`           | Secure cron endpoint                   |

### Pull from Vercel

After linking your project, pull all environment variables:

```bash
vercel link
vercel env pull .env.local
```

## Rate Limiting and Security

The authenticated EVE gateway is expensive because it can call AI providers. Protect its routes from abuse in production.

### Vercel Firewall

Enable [Vercel Firewall](https://vercel.com/docs/security/vercel-firewall) in your project settings. Create a rate limiting rule for the chat endpoint:

1. Go to **Settings** → **Firewall**
2. Add a rule targeting the authenticated EVE gateway routes
3. Set a rate limit (for example, 20 requests per minute per IP)
4. Choose **Challenge** or **Block** as the action

### Vercel WAF

For additional protection, enable the [Web Application Firewall](https://vercel.com/docs/security/vercel-waf) to block common attack patterns (SQL injection, XSS) at the edge before requests reach your application.

### Environment Variable Security

- Never commit `.env.local` to version control
- Use Vercel's [environment variable management](https://vercel.com/docs/projects/environment-variables) to set secrets per environment (production, preview, development)
- Rotate `AUTH_SECRET` and API keys periodically

## Production Checklist

Before going live:

<Accordion>
  <AccordionItem title="Enable production domain">
    1. Go to **Settings** → **Domains** 2. Add your custom domain 3. Update
    OAuth callback URLs to use the new domain
  </AccordionItem>
  <AccordionItem title="Configure OAuth for production">
    Update your OAuth apps (GitHub, Google) with production callback URLs: ```
    https://yourdomain.com/api/auth/callback/github
    https://yourdomain.com/api/auth/callback/google ```
  </AccordionItem>
  <AccordionItem title="Enable rate limiting">
    Set up [Vercel Firewall](#vercel-firewall) rules for the EVE gateway to
    prevent abuse. See the section above for details.
  </AccordionItem>
  <AccordionItem title="Set up monitoring">
    - Enable [Vercel Analytics](https://vercel.com/analytics) for web vitals -
    Configure [Langfuse](https://langfuse.com) for LLM observability (optional)
  </AccordionItem>
  <AccordionItem title="Review storage limits">
    Check your plan limits for blob storage, EVE workflow storage, and AI
    Gateway usage.
  </AccordionItem>
</Accordion>

## Troubleshooting

<Accordion>
  <AccordionItem title="Cron job not running">
    - Verify `CRON_SECRET` is set in environment variables - Check Vercel
    dashboard → **Logs** → filter by `/api/cron` - Crons only run in production
    (not preview deployments)
  </AccordionItem>
  <AccordionItem title="Blob upload fails">
    - Ensure `BLOB_READ_WRITE_TOKEN` is set - Check blob storage isn't at
    capacity - Verify file size is under 500MB limit
  </AccordionItem>
  <AccordionItem title="Code execution times out">
    - Default timeout is 5 minutes - Check sandbox logs in Vercel dashboard -
    Ensure OIDC is working (automatic on Vercel)
  </AccordionItem>
</Accordion>
