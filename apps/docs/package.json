{
  "name": "@chatjs/docs",
  "private": true,
  "engines": {
    "node": ">=22 <25"
  },
  "volta": {
    "node": "22.22.2"
  },
  "scripts": {
    "build": "blume build",
    "dev": "blume dev --port ${PORT:-4321}",
    "preview": "blume preview",
    "docs:links": "blume validate --external --strict",
    "docs:audit": "blume audit",
    "docs:eval": "blume eval",
    "lint": "blume doctor",
    "test:visual": "playwright test",
    "uiverify": "uiverify upload --static-dir uiverify-archive"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@uiverify/playwright": "^1.0.2",
    "blume": "^1.3.1",
    "uiverify": "^1.0.2"
  }
}
