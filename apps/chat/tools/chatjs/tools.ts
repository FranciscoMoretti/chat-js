// Generated by chat-js sync. Put custom registrations in custom-tools.ts / custom-ui.ts.
// Content: 796c55bd2b47f03ed71bddc04a94c3b45b37c8517a5a0608de3e6e055455b69d
import type { ToolSet } from "ai";
import { customTools } from "./custom-tools";
import { getWeather as tool0 } from "./get-weather/tool";
import { retrieveUrl as tool1 } from "./retrieve-url/tool";
import { webSearch as tool2 } from "./tavily-search/tool";
import { codeExecution as tool3 } from "./vercel-code-execution/tool";
import { wordCount as tool4 } from "./word-count/tool";

const installed = {
  getWeather: tool0,
  retrieveUrl: tool1,
  webSearch: tool2,
  codeExecution: tool3,
  wordCount: tool4,
} satisfies ToolSet;
for (const key of Object.keys(customTools)) {
  if (Object.hasOwn(installed, key)) {
    throw new Error(`Duplicate tool registration: ${key}`);
  }
}
export const tools = { ...installed, ...customTools };
