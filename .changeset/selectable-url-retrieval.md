---
"@chat-js/cli": minor
"@chat-js/registry": minor
---

Select URL retrieval tools during creation using the retrieveUrl slot. Install only the selected provider and derive its credential requirements from registry metadata.
