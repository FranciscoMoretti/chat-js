diff --git a/src/execution/workflow-runtime.ts b/src/execution/workflow-runtime.ts
index 3e92099..b5fbbed 100644
--- a/src/execution/workflow-runtime.ts
+++ b/src/execution/workflow-runtime.ts
@@ -234,6 +234,10 @@ export function createWorkflowRuntime(config: {
             });
       const attributes = {
         ...sessionAttributes,
+        // Resource inventory must remain readable without resolving session input.
+        ...(collectorRunId === undefined
+          ? {}
+          : { "$eve.activity_collector": collectorRunId }),
         ...(input.externalInvocation === undefined
           ? {}
           : buildInvocationAttributes(input.externalInvocation)),
diff --git a/src/execution/workflow-runtime.test.ts b/src/execution/workflow-runtime.test.ts
index 45f619f..55769ab 100644
--- a/src/execution/workflow-runtime.test.ts
+++ b/src/execution/workflow-runtime.test.ts
@@ -592,6 +592,10 @@ describe("createWorkflowRuntime#createSession", () => {
     expect(collectorInput.token).toHaveLength(43);
     const workflowInput = startMock.mock.calls[1]?.[1][0];
     expect(workflowInput.activityCollectorRunId).toBe("collector-run");
+    expect(startMock.mock.calls[1]?.[2]).toMatchObject({
+      allowReservedAttributes: true,
+      attributes: { "$eve.activity_collector": "collector-run" },
+    });
     expect(workflowInput.serializedContext[ActivityObserverKey.name]).toEqual({
       sink: {
         url: `https://agent.example.com/eve/v1/activity/${collectorInput.token}`,
